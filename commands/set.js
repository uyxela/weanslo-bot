const { prefix } = require("../config.json");

module.exports = {
  name: "set",
  description: "Sets the status of a hackathon",
  usage: "<id> current | submitted <url>",
  execute(message, args) {}
};
